import { ModulePlaceholder } from "@/components/shared/module-placeholder";

export const metadata = { title: "Query Form" };

export default function AdminQueryFormPage() {
  return (
    <ModulePlaceholder
      title="Query Form"
      description="Review and respond to user queries."
    />
  );
}
