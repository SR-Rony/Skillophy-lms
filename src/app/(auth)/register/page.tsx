import { RegisterPageContent } from "@/components/auth";

export const metadata = {
  title: "Create Account",
  description: "Create your Skillophy account and start your learning journey.",
};

export default function RegisterPage() {
  return <RegisterPageContent />;
}
