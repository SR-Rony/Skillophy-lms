import { LoginPageContent } from "@/components/forms/login-form";

export const metadata = {
  title: "Sign in",
  description: "Sign in to your Skillophy account.",
};

export default function LoginPage() {
  return <LoginPageContent />;
}
